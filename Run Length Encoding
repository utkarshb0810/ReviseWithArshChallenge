string encode(string s) {
        int n = s.size();
        string ans = "";
        for(int i =0;i< s.size();i++){
            int cnt = 1;
            while(i+1 < n and s[i] == s[i+1]){
                cnt++;
                i++;
            }
            ans += s[i];
            ans += to_string(cnt);
        }
        return ans;
    }
